# Backend API Configuration
# Bu dosyayı .env olarak kopyalayın ve değerleri güncelleyin

# Ollama API URL (Docker içinde servis adı kullanılır)
OLLAMA_API_URL=http://beyin-ollama:11434

# Veritabanı yolu (SQLite)
DATABASE_URL=./data/veritabani.db

# JWT Secret Key (MUTLAKA DEĞİŞTİRİN!)
JWT_SECRET_KEY=COK_GIZLI_BIR_ANAHTAR_BITIRME_PROJESI

# Proje dosyalarının saklanacağı klasör
PROJECTS_PATH=/app/projects

# Gin Mode (release veya debug)
GIN_MODE=release

# Port (varsayılan: 8000)
PORT=8000
